pkgname="carbonio-elixir"
pkgver="1.17.2" # renovate: datasource=github-tags depName=elixir-lang/elixir
pkgrel="1"
pkgdesc="A functional meta-programming aware language built on top of the Erlang VM"
url="https://www.zextras.com/"
maintainer="Zextras <packages@zextras.com>"
license=("Apache-2.0")
section="utils"
priority="optional"
arch=('x86_64')
depends=("carbonio-erlang")
source=(
  "https://github.com/elixir-lang/elixir/archive/v${pkgver}.tar.gz"
)
sha256sums=('7bb8e6414b77c1707f39f620a2ad54f68d64846d663ec78069536854247fb1ab')

build() {
  export PATH="/opt/zextras/common/bin/:$PATH"

  cd "${srcdir}/elixir-${pkgver}"
  make -j8
}

package() {
  export PATH="/opt/zextras/common/bin/:$PATH"

  cd "${srcdir}/elixir-${pkgver}"
  make DESTDIR="${pkgdir}" PREFIX=/usr install
  rm -rf "${pkgdir}"/usr/share/man
}
